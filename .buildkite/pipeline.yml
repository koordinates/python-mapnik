---
steps:
  - label: ':debian: Build'
    command: ".buildkite/build.sh"
    artifact_paths:
      - "build-bionic/*"

  - block: "Release?"
    prompt: "Tag & Release to archive?"

  - label: ':git: Add git tag'
    command: .buildkite/tag.sh
    plugins:
      docker#v1.4.0:
        image: "${ECR}/ci-tools"
        always-pull: true
        workdir: /src
        environment:
          - GITHUB_TOKEN
          - BUILDKITE_COMMIT
          - BUILDKITE_BUILD_NUMBER
          - BUILDKITE_BRANCH

  - label: ':debian: Publish deb'
    command:
      - >
        aptly-upload
        --aptly-url https://apt-repo.kx.gd
        --retries 3
        --repo kx-builds-bionic
        --series bionic
        build-bionic/*.deb
    retry:
      automatic: true
    plugins:
      artifacts#v1.2.0:
        download: "build-bionic/*.deb"
      docker#v1.4.0:
        image: "${ECR}/ci-tools"
        always-pull: true
        workdir: /src
        environment:
          - APTLY_UNAME
          - APTLY_PASSWD
